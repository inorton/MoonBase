<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

             >

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>


    <Button Content="Refresh" Command="{Binding Path=RefreshCommand}"
      Grid.Column="0" Grid.Row="0"/>
    <ProgressBar IsIndeterminate="{Binding Path=Refreshing}"
      Grid.Column="1" Grid.Row="0" MinHeight="20" Width="20"/>


    <ListBox ItemsSource="{Binding Path=Servers}"
      Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2">
      <ListBox.ItemTemplate>
        <DataTemplate>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>

            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding Path=Address}" Grid.Column="0">
              <ToolTipService.ToolTip>
                <TextBlock Text="{Binding Path=Lifetime}"/>
              </ToolTipService.ToolTip>
            </TextBlock>



          </Grid>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>

  </Grid>
             

</UserControl>
